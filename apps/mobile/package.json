{
  "name": "@sms/mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Expo React Native client for teachers (placeholder — fleshed out in Epic E14)",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "echo 'apps/mobile dev not wired yet (Epic E14)'",
    "test": "jest --passWithNoTests",
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rimraf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@sms/core-logic": "workspace:*"
  }
}
