/**
 * apps/mobile — Expo React Native (Android first). Placeholder.
 *
 * Real Expo entry, navigation, attendance capture, sync button, and M-Pesa
 * payment modal land in Epic E14.
 */
import { CORE_LOGIC_VERSION } from "@sms/core-logic";

export const MOBILE_VERSION = "0.1.0";
export const CORE_LOGIC = CORE_LOGIC_VERSION;
