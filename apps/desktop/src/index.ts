/**
 * apps/desktop — Electron + React shell. Placeholder.
 *
 * Real Electron main process, Vite + React renderer, and the admin UI
 * (login, dashboard, students, users, access log) land in Epic E13.
 */
import { CORE_LOGIC_VERSION } from "@sms/core-logic";

export const DESKTOP_VERSION = "0.1.0";
export const CORE_LOGIC = CORE_LOGIC_VERSION;
